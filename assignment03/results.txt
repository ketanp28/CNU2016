QUERY1:
SELECT ORDERS.ORDER_DATE, SUM(ORDER_DETAILS.QUANTITY_ORDERED*ORDER_DETAILS.SELLING_PRICE)
FROM ORDERS JOIN ORDER_DETAILS ON ORDER_DETAILS.ORDER_ID=ORDERS.ORDER_ID GROUP BY ORDERS.ORDER_DATE;

before indexing = 1.1 sec
after indexing = 312 ms

QUERY2:
SELECT ORDERS.ORDER_DATE, SUM(ORDER_DETAILS.QUANTITY_ORDERED*(ORDER_DETAILS.SELLING_PRICE-ORDER_DETAILS.COST_PRICE))
FROM ORDERS JOIN ORDER_DETAILS ON ORDER_DETAILS.ORDER_ID=ORDERS.ORDER_ID GROUP BY ORDER_DATE;

before indexing = 432 ms
after indexing = 231 ms

QUERY3:
SELECT USER_DETAILS.COMPANY_NAME, SUM(ORDER_DETAILS.QUANTITY_ORDERED)/COUNT(ORDER_DETAILS.SELLING_PRICE-ORDER_DETAILS.COST_PRICE))
FROM USER_DETAILS JOIN ORDERS ON USER_DETAILS.USER_ID=ORDERS.USER_ID JOIN ORDER_DETAILS ON ORDER_DETAILS.ORDER_ID=ORDERS.ORDER_ID GROUP BY USER_DETAILS.COMPANY_NAME;

before indexing = 1.2 sec
after indexing = 331 ms
